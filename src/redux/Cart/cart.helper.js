import { firestore } from "../../firebase/ultils";

export const handleAddCart = cart => {
    // return new Promise((resolve, reject) => {
    //     firestore
    //         .collection('cart')
    //         .doc()
    //         .set({cart})
    //         .then(() => {
    //             resolve()
    //         })
    //         .catch(err => {
    //             reject(err);
    //         })
    // });
}